# Pró Porco - Configuração de Variáveis de Ambiente
# Copie este arquivo para .env e preencha com os valores corretos

# =============================================================================
# BANCO DE DADOS
# =============================================================================
# URL de conexão com PostgreSQL (Neon ou outro provider)
# Formato: postgresql://user:password@host:port/database?sslmode=require
# Exemplo: postgresql://user:pass@ep-example.us-east-2.aws.neon.tech/prorporco?sslmode=require
DATABASE_URL=postgresql://user:password@host:port/database

# =============================================================================
# AUTENTICAÇÃO
# =============================================================================
# Chave secreta para assinatura de tokens JWT
# IMPORTANTE: Use uma string aleatória e segura em produção
# Exemplo de geração: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# =============================================================================
# AMBIENTE
# =============================================================================
# Define o ambiente de execução (development, production, test)
# Em produção, isso ativa cookies seguros (HTTPS) e outras otimizações
NODE_ENV=development

# =============================================================================
# SERVIDOR
# =============================================================================
# URL do frontend para configuração de CORS
# Em desenvolvimento, use http://localhost:5000
# Em produção, use a URL do seu domínio (ex: https://app.prorporco.com)
FRONTEND_URL=http://localhost:5000

# Porta do servidor Express (padrão: 3000)
# PORT=3000

# Porta do servidor Vite (padrão: 5000)
# VITE_PORT=5000
